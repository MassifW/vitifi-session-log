# Status audit — 13 September 2026

Went through the project's own decision log in full and swept the codebase directly for everything flagged, deferred, not built, or left as a known limitation — cross-checked the highest-stakes claims against the live database rather than trusting the doc. Also closed six previously-open questions in the same session. Full detail in the private log; this is the filtered summary.

## Headline findings

- One of the app's own recommendation features turned out to be running entirely on hardcoded placeholder data end to end, disconnected from the real database it should be reading from. The code sweep found this; it wasn't previously written down this plainly.
- No authentication or access-control policies exist yet anywhere in the app — this is the one hard blocker on deploying it anywhere reachable from the internet.
- A large piece of cellar/winemaking data infrastructure is fully designed in the database but has zero code or screen built against it yet — the single biggest "built ahead of the feature" gap in the app.
- A planned certification/compliance check is documented as an intention but hasn't been started.
- Two things the decision log claimed were true turned out to be stale once checked directly against the live data — one because more work happened after that entry was written, one that turned out to already be resolved.

## What's blocked on what

- **Most items** are waiting on engineering work only — nothing external is stopping them.
- **A handful** are waiting on outside parties: a vendor's API key still hasn't arrived, a couple of published reference datasets don't exist yet, and one product's compliance status needs a registration lookup.
- **A few** are waiting on a decision only Michael can make — reconciling a design-versus-reality discrepancy, and whether to correct some already-imported records to a finer level of detail than the source data itself used.

## Closed today

Six previously-open questions were answered and recorded: a historic boundary explained, one area's status confirmed directly, some equipment capacity data located and recorded, a vendor's deliverable scope confirmed complete, a handful of product units confirmed and corrected, and a data-corruption case (a spreadsheet auto-converting a text value into a date) fully traced, confirmed to affect only the one already-known instance, and confirmed already safe in the database — with the export-side protection for this exact failure mode (built in an earlier session) verified to already cover it correctly. One small new item surfaced needing a physical field check.

## Waiting on Michael

- One boundary-reconciliation decision.
- One decision on whether to correct some already-imported records to a finer split than the source data used.
- A vendor key swap before an expiry date later this year.
- Confirming a small number of missing reference-data correspondences, when available.

## Also today

This is the first entry to either log since 4 September — the habit had lapsed. A standing instruction is now in the project's own guidance file so it doesn't lapse again.
